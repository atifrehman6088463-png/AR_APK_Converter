<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app">

    <!--
        INTERNET: for any future remote features.
        No WRITE_EXTERNAL_STORAGE or READ_EXTERNAL_STORAGE is declared —
        KhataPrintManager uses internal app storage + FileProvider for sharing,
        which requires zero storage permissions on all Android versions.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            FileProvider — enables KhataPrintManager.buildShareIntent() to share
            PDFs from internal storage without any storage permissions.

            android:authorities must match the value passed to
            KhataPrintManager.buildShareIntent(bill, authority = "…").

            android:grantUriPermissions="true" lets the Intent carry
            FLAG_GRANT_READ_URI_PERMISSION so the receiving app (Gmail,
            WhatsApp, etc.) can read the file for the duration of the request.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
